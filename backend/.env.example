PORT=5000
NODE_ENV=development

# MySQL
DB_HOST=localhost
DB_NAME=coachcode_db
DB_USER=root
DB_PASSWORD=180506

# JWT (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES=7d

# Optional: frontend URL for CORS (comma-separated for multiple)
CORS_ORIGIN=http://localhost:5173

# Judge0 (optional - uses public ce.judge0.com if not set)
# JUDGE0_BASE=https://ce.judge0.com
# JUDGE0_API_KEY=your-rapidapi-key-if-required
